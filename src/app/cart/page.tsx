import Cart from '../../components/Cart';

export default function CartPage() {
  return <main style={{ minHeight: '100vh', paddingBottom: '76px', background: 'var(--bg-darker)' }}><Cart pageMode /></main>;
}
